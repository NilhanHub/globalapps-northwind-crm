# Normalize line endings: LF in the repo, OS-native in the working copy.
# Keeps diffs clean across Windows/Mac/Linux contributors.
* text=auto eol=lf

# Binary files — never normalize
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.pdf binary
*.zip binary
