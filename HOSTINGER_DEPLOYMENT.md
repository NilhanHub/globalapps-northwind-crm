# Hostinger deployment pointer

This root filename is retained only for compatibility with older links and agent instructions.

The single authoritative deployment runbook is [docs/HOSTINGER_DEPLOYMENT.md](docs/HOSTINGER_DEPLOYMENT.md). It describes the current Hostinger managed Node.js applications, Firestore repository, staging boundary, exact-SHA promotion workflow, rollback procedure and encrypted backup cron.

Do not reconstruct the retired VPS, Nginx or production-JSON deployment from Git history. Confirm the active runbook and current environment before any hosting or cloud mutation.
